# Render Blueprint — deploys all services from one file
# https://render.com/docs/blueprint-spec

services:
  # FastAPI Backend
  - type: web
    name: docmind-backend
    runtime: docker
    region: oregon
    plan: free
    rootDir: backend
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: docmind-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: docmind-redis
          type: redis
          property: connectionString
      - key: QDRANT_URL
        sync: false
      - key: QDRANT_API_KEY
        sync: false
      - key: LLM_API_KEY
        sync: false
      - key: LLM_BASE_URL
        value: https://api.groq.com/openai/v1
      - key: LLM_MODEL
        value: llama-3.3-70b-versatile
      - key: LLM_FAST_MODEL
        value: llama-3.1-8b-instant
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: '["https://docmind-frontend.onrender.com"]'
      - key: APP_ENV
        value: production
      - key: DEBUG
        value: "false"

  # Next.js Frontend
  - type: web
    name: docmind-frontend
    runtime: node
    region: oregon
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://docmind-backend.onrender.com

  # Redis
  - type: redis
    name: docmind-redis
    region: oregon
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []

# PostgreSQL Database
databases:
  - name: docmind-db
    region: oregon
    plan: free
    databaseName: docmind
    user: docmind
